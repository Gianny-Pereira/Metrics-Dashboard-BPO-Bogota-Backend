Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
Flask-CORS==4.0.1
python-dotenv==1.0.1
openpyxl==3.1.5
requests==2.32.3

# PostgreSQL driver — only needed when DATABASE_URL points to Postgres.
# If this fails on your machine, install it separately: pip install psycopg2-binary
psycopg2-binary==2.9.9
